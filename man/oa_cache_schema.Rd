% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oa_schema.R
\name{oa_cache_schema}
\alias{oa_cache_schema}
\title{Populate the local baseline-schema cache from a snapshot metadata directory}
\usage{
oa_cache_schema(source, entities = "all", overwrite = FALSE, verbose = TRUE)
}
\arguments{
\item{source}{Path to the snapshot metadata directory, e.g.
\code{"/Volumes/openalex/openalex-snapshot_metadata"}.}

\item{entities}{Character vector of entity names to cache, or \code{"all"}
(default) to cache every entity directory found under \code{source}.}

\item{overwrite}{Logical.  Overwrite an existing cached file?  Default
\code{FALSE}.}

\item{verbose}{Logical.  Print progress messages?  Default \code{TRUE}.}
}
\value{
The path to the schemata cache directory (invisibly).
}
\description{
Copies \code{unified_schema.csv} files from an OpenAlex snapshot metadata
directory (e.g. \code{/Volumes/openalex/openalex-snapshot_metadata}) into
the user-level cache used by \code{\link{pro_request_parquet}(schema = "auto")}.
}
\details{
Once cached, the schemas are used even when the source volume is not mounted.
Update the cache periodically to pick up new fields added by OpenAlex (run
with \code{overwrite = TRUE}).
}
\seealso{
\code{\link{pro_request_parquet}} for the \code{schema} parameter.
}
